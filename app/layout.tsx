import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'UltraMedia Studio — Live race intelligence',
  description:
    'An AI-powered newsroom for live ultramarathon storytelling, verified race intelligence, and broadcast-ready coverage.',
  openGraph: {
    title: 'UltraMedia Studio',
    description: 'Every mile becomes a story.',
    images: ['/ultramedia-og.png'],
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
